<p id="notice"><%= notice %></p>
<p>
  <strong>Name:</strong>
  <%= @flat.name %>
  <%= image_tag @flat.photo.url(:bright_face) %>

</p>

<p>
  <strong>Address:</strong>
  <%= @flat.address %>
</p>

<%= link_to 'Edit', edit_flat_path(@flat) %> |
<%= link_to 'Back', flats_path %>
